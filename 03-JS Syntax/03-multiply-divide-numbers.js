function multiplyDivideNumbers(numbers) {
    if (numbers[0] <= numbers[1]) {
        return numbers[0] * numbers[1];
    } else {
        return numbers[0] / numbers[1];
    }
}

// console.log(multiplyDivideNumbers(["2", "3"]));
// console.log(multiplyDivideNumbers(["13", "13"]));
// console.log(multiplyDivideNumbers(["3", "2"]));
// console.log(multiplyDivideNumbers(["144", "12"]));
