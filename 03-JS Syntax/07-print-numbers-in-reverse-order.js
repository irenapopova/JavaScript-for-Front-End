function printInReverseOrder(numbers) {
    for (let i = numbers.length - 1; i >= 0; i--) {
        console.log(numbers[i]);
    }
}

// printInReverseOrder(["10", "15", "20"]);
// printInReverseOrder(["5", "5.5", "24", '-3']);
// printInReverseOrder(["20", "1", "20", "1", "20"]);
